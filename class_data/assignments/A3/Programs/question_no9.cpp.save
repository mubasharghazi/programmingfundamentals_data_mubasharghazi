#include <iostream>
using namespace std;
int main() {
    int n;
    cout<<"Enter Number of Rows: "<<endl;
    cin
  // For each row
  for (int i = 1; i <= n; ++i) {
    for (int j = 1; j <= 2 * i - 1; ++j) {
      cout << "*";
    }
    cout << endl; // New line after each row
  }

  return 0;
}
